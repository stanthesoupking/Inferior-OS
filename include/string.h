#pragma once

/**
 * Calculate the length of a given null-terminated string.
 * 
 * Parameters:
 *  str - Target String
 * 
 * Returns:
 *  Number of characters the given string
 */
int strlen(const char *str);